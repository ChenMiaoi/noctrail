# PTY Fixtures

This directory is reserved for PTY smoke inputs and lifecycle fixtures.

The current baseline uses the `noctrail pty-smoke` command as the
cross-check entrypoint while the runtime event model is still being
solidified.
